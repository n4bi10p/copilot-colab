# Copilot CoLab

VS Code extension for hackathon teamwork: tasks, chat, presence, AI agents, and reporting.

## Setup

- See project_structure.md and PRD for requirements.
- Run `npm install` and `npm run build` to start.
